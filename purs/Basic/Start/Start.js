import { Html, Head, Title, Meta, Body, Scripts, Routes, ErrorBoundary } from 'solid-start';
import { Suspense } from 'solid-js';

export const html_ = Html;
export const head_ = Head;
export const title_ = Title;
export const meta_ = Meta;
export const body_ = Body;
export const scripts_ = Scripts;
export const routes_ = Routes;
export const errorBoundary_ = ErrorBoundary;
export const suspense_ = Suspense;